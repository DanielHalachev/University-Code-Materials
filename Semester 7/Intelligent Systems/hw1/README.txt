Архивът съдържа две решения. 
Първото, по-бързо решение, (solution), използва std::vector за преминаване през децата, за разлика от второто (alternative solution), което използва std::priority_queue. Изпозването на std::unordered_set за посетените състояния (вместо проверка в цялата структура path) не предложи по-добър резултат, поради което не съм предоставил този вариант на решението. 
За изписване на времето за изчисление, подайте произволен параметър, при изпълнение на изпълнимия файл. Например, `./main.out a`. При неподаден параметър, програмата не изписва времето за изчисление.

